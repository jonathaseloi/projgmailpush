<%= form_with(model: pubsub) do |form| %>
  <% if pubsub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pubsub.errors.count, "error") %> prohibited this pubsub from being saved:</h2>

      <ul>
        <% pubsub.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
